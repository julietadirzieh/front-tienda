const ProductCategoriesContainer = ({ children }) => {
  return (
    <section className="w-full bg-white rounded-xl p-6 my-12 border border-black">
      {children}
    </section>
  );
};

export default ProductCategoriesContainer;
